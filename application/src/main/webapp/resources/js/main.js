/**
 * EmployeeApp should be the namespace for all javascript to
 * register itself with in this basic Employee Application
 * using the module pattern
 */
var EmployeeApp;

// Wire up tooltips on page load
$(function () {
    $(document).tooltip({items: "[title]"});
})