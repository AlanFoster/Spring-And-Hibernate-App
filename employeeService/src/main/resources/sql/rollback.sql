--
-- WARNING : Used to safely rollback ALL tables etc from the database
--

-- Drop the created jobs table if it exists
drop table if exists jobs;

-- Drop the created employees table if it exists
drop table if exists employees;
